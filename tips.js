//variables create as inputs for calculation
let foodCost = 79.25;
let tax = 6.54;
let tip = 12.00;

//do our calculations for the variables above
let totalDue = foodCost + tax + tip;

//output the results to the console
console.log ("food cost is $" + foodCost + " and the tax is $" +tax);
console.log ("tip is $" + tip.toFixed(2));
console.log ("the total due is $" + totalDue);

